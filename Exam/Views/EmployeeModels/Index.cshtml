@model IEnumerable<Exam.Models.EmployeeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeRank)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeRank)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
