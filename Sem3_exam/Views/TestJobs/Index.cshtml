@model IEnumerable<Sem3_exam.Mains.Models.TestJob>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.department_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.number_of_personals)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.department_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.number_of_personals)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.department_code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.department_code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.department_code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
