@model IEnumerable<Sem3_exam.Mains.Models.TestUser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.employee_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeJob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.employee_rnk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.employee_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypeJob.department_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.employee_rnk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.employee_code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.employee_code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.employee_code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
